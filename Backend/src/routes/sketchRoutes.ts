import express, { Request, Response, NextFunction } from "express";

const router = express.Router();

// router.route("/").get();

// router.route("/:id").post().get().delete().patch();

export default router;
